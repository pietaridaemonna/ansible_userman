extends layout_new

block body
  br
  br
  .main.container
    .row
      .col-md-6.col-md-offset-3
        h1.display-4.m-b-2 Teams

        form(method='POST' action='/create_team')
          div.form-group
            label(for='username') teamname
            input.form-control(type='text' id='teamname' placeholder='teamname' name='teamname')
          div.form-group
            label(for='password') unix groups
            input.form-control(type='text' id='nixgroups' placeholder='unix groups' name='nixgroups')

          div.form-group
            label(for='add_keys_to_user') add_keys_to_user
            input.form-control(type='textarea' id='add_keys_to_user' placeholder='unix user where to add keys' name='add_keys_to_user')
          div.form-group
            label(for='teams') already existing teams
            p
            //select(name='teams', multiple='')
            //  each val in teams
            //    option(value='#{val}') #{val}
          p
          button.btn.btn-primary(type='submit') Create
